<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Yggdrassil.Presentation.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Yggdrassil.Presentation"
    xmlns:pages="using:Yggdrassil.Presentation.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <NavigationView
        x:Name="MainNavigationView"
        IsBackButtonVisible="Auto"
        IsSettingsVisible="False"
        PaneDisplayMode="Left"
        SelectionChanged="MainNavigationView_SelectionChanged">
        <NavigationView.MenuItems>
            <NavigationViewItem x:Name="HomeNavigationItem" Content="Home" Tag="home" />
            <NavigationViewItem x:Name="ProjectNavigationItem" Content="Project" Tag="project" />
            <NavigationViewItem x:Name="ModelNavigationItem" Content="Model" Tag="model" />
            <NavigationViewItem x:Name="RiggingNavigationItem" Content="Rigging" Tag="rigging" />
            <NavigationViewItem x:Name="ExportNavigationItem" Content="Export" Tag="export" />
            
        </NavigationView.MenuItems>
        <Grid Padding="16" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <Frame x:Name="ContentFrame" Grid.Column="0" />

            <Border
                Style="{StaticResource PanelBorderStyle}"
                Grid.Column="1"
                Padding="20">
                <Grid RowSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Spacing="4">
                        <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="Viewport" />
                        <TextBlock
                            Style="{StaticResource CaptionTextStyle}"
                            Text="Persistent visual debugging panel for mesh, skeleton, and transform inspection." />
                    </StackPanel>

                    <Border
                        Style="{StaticResource SubtlePanelBorderStyle}"
                        Grid.Row="1"
                        MinHeight="420"
                        Margin="0,8,0,0"
                        Background="{ThemeResource AppPanelSubtleBackgroundBrush}">
                        <Grid>
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyStrongTextStyle}"
                                Text="Vortice viewport host goes here" />
                        </Grid>
                    </Border>

                    <Border
                        Style="{StaticResource SubtlePanelBorderStyle}"
                        Grid.Row="2"
                        Margin="0,8,0,0"
                        Padding="16">
                        <StackPanel Spacing="6">
                            <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Scene Details" />
                            <TextBlock Text="Bone tree, root orientation, and selection details will live here." TextWrapping="WrapWholeWords" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </NavigationView>
</Window>
