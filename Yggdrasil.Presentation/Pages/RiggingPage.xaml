<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Yggdrasil.Presentation.Pages.RiggingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:Yggdrasil.Presentation.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Yggdrasil.Presentation.Models"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:ThicknessScaleConverter x:Key="BoneHierarchyIndentationConverter" Scale="0.55" />
        <converters:BooleanToBrushConverter
            x:Key="DuplicateAssignmentBackgroundConverter"
            FalseBrush="{ThemeResource AppPanelSubtleBackgroundBrush}"
            TrueBrush="{ThemeResource AppValidationErrorBackgroundBrush}" />
        <converters:BooleanToBrushConverter
            x:Key="DuplicateAssignmentBorderConverter"
            FalseBrush="{ThemeResource AppPanelBorderBrush}"
            TrueBrush="{ThemeResource AppValidationErrorBorderBrush}" />
        <converters:BooleanToBrushConverter
            x:Key="DuplicateAssignmentForegroundConverter"
            FalseBrush="{ThemeResource TextFillColorSecondaryBrush}"
            TrueBrush="{ThemeResource AppValidationErrorForegroundBrush}" />
        <converters:BooleanToBrushConverter
            x:Key="SelectedSlotBackgroundConverter"
            FalseBrush="Transparent"
            TrueBrush="{ThemeResource AppSelectionBackgroundBrush}" />
        <converters:BooleanToBrushConverter
            x:Key="SelectedSlotBorderConverter"
            FalseBrush="Transparent"
            TrueBrush="{ThemeResource AppSelectionBorderBrush}" />
        <Thickness x:Key="TreeViewItemPresenterMargin">0</Thickness>
        <Thickness x:Key="TreeViewItemPresenterPadding">0</Thickness>
        <x:Double x:Key="TreeViewItemMinHeight">18</x:Double>
        <x:Double x:Key="TreeViewItemContentHeight">16</x:Double>
        <Style x:Key="RigSectionExpanderStyle" TargetType="Expander">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="FontSize" Value="12" />
        </Style>
        <Style x:Key="RigSlotAssignedFieldStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource AppPanelSubtleBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource AppPanelBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Padding" Value="8,6" />   
            <Setter Property="HorizontalAlignment" Value="Stretch" />
        </Style>
        <Style x:Key="BoneHierarchyTreeItemStyle" TargetType="TreeViewItem">
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="2,0,0,0" />
            <Setter Property="MinHeight" Value="{StaticResource TreeViewItemMinHeight}" />
            <Setter Property="GlyphSize" Value="8" />
            <Setter Property="FocusVisualMargin" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid
                            x:Name="ContentPresenterGrid"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Margin="{ThemeResource TreeViewItemPresenterMargin}"
                            Padding="{ThemeResource TreeViewItemPresenterPadding}"
                            CornerRadius="{TemplateBinding CornerRadius}">

                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenterGrid.Background" Value="{ThemeResource TreeViewItemBackgroundPointerOver}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TreeViewItemForegroundPointerOver}" />
                                            <Setter Target="SelectionIndicator.Fill" Value="{ThemeResource TreeViewItemSelectionIndicatorForegroundPointerOver}" />
                                            <Setter Target="CollapsedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundPointerOver}" />
                                            <Setter Target="ExpandedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundPointerOver}" />
                                            <Setter Target="ContentPresenterGrid.BorderBrush" Value="{ThemeResource TreeViewItemBorderBrushPointerOver}" />
                                            <Setter Target="SelectionIndicator.Opacity" Value="0" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenterGrid.Background" Value="{ThemeResource TreeViewItemBackgroundPressed}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TreeViewItemForegroundPressed}" />
                                            <Setter Target="SelectionIndicator.Fill" Value="{ThemeResource TreeViewItemSelectionIndicatorForegroundPressed}" />
                                            <Setter Target="CollapsedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundPressed}" />
                                            <Setter Target="ExpandedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundPressed}" />
                                            <Setter Target="ContentPresenterGrid.BorderBrush" Value="{ThemeResource TreeViewItemBorderBrushPressed}" />
                                            <Setter Target="SelectionIndicator.Opacity" Value="0" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Selected">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenterGrid.Background" Value="{ThemeResource TreeViewItemBackgroundSelected}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TreeViewItemForegroundSelected}" />
                                            <Setter Target="SelectionIndicator.Fill" Value="{ThemeResource TreeViewItemSelectionIndicatorForeground}" />
                                            <Setter Target="CollapsedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundSelected}" />
                                            <Setter Target="ExpandedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundSelected}" />
                                            <Setter Target="ContentPresenterGrid.BorderBrush" Value="{ThemeResource TreeViewItemBorderBrushSelected}" />
                                            <Setter Target="SelectionIndicator.Opacity" Value="1" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenterGrid.Background" Value="{ThemeResource TreeViewItemBackgroundDisabled}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TreeViewItemForegroundDisabled}" />
                                            <Setter Target="SelectionIndicator.Fill" Value="{ThemeResource TreeViewItemSelectionIndicatorForegroundDisabled}" />
                                            <Setter Target="CollapsedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundDisabled}" />
                                            <Setter Target="ExpandedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundDisabled}" />
                                            <Setter Target="ContentPresenterGrid.BorderBrush" Value="{ThemeResource TreeViewItemBorderBrushDisabled}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="PointerOverSelected">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenterGrid.Background" Value="{ThemeResource TreeViewItemBackgroundSelectedPointerOver}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TreeViewItemForegroundSelectedPointerOver}" />
                                            <Setter Target="SelectionIndicator.Fill" Value="{ThemeResource TreeViewItemSelectionIndicatorForegroundPointerOver}" />
                                            <Setter Target="CollapsedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundSelectedPointerOver}" />
                                            <Setter Target="ExpandedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundSelectedPointerOver}" />
                                            <Setter Target="ContentPresenterGrid.BorderBrush" Value="{ThemeResource TreeViewItemBorderBrushSelectedPointerOver}" />
                                            <Setter Target="SelectionIndicator.Opacity" Value="1" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="PressedSelected">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenterGrid.Background" Value="{ThemeResource TreeViewItemBackgroundSelectedPressed}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TreeViewItemForegroundSelectedPressed}" />
                                            <Setter Target="SelectionIndicator.Fill" Value="{ThemeResource TreeViewItemSelectionIndicatorForegroundPressed}" />
                                            <Setter Target="CollapsedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundSelectedPressed}" />
                                            <Setter Target="ExpandedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundSelectedPressed}" />
                                            <Setter Target="ContentPresenterGrid.BorderBrush" Value="{ThemeResource TreeViewItemBorderBrushSelectedPressed}" />
                                            <Setter Target="SelectionIndicator.Opacity" Value="1" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="SelectedDisabled">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenterGrid.Background" Value="{ThemeResource TreeViewItemBackgroundSelectedDisabled}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TreeViewItemForegroundSelectedDisabled}" />
                                            <Setter Target="SelectionIndicator.Fill" Value="{ThemeResource TreeViewItemSelectionIndicatorForegroundDisabled}" />
                                            <Setter Target="CollapsedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundSelectedDisabled}" />
                                            <Setter Target="ExpandedGlyph.Foreground" Value="{ThemeResource TreeViewItemForegroundSelectedDisabled}" />
                                            <Setter Target="ContentPresenterGrid.BorderBrush" Value="{ThemeResource TreeViewItemBorderBrushSelectedDisabled}" />
                                            <Setter Target="SelectionIndicator.Opacity" Value="1" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="ReorderedPlaceholder">
                                        <Storyboard>
                                            <FadeOutThemeAnimation TargetName="MultiSelectGrid" />
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="TreeViewMultiSelectStates">
                                    <VisualState x:Name="TreeViewMultiSelectDisabled" />
                                    <VisualState x:Name="TreeViewMultiSelectEnabledUnselected">
                                        <VisualState.Setters>
                                            <Setter Target="MultiSelectCheckBox.Visibility" Value="Visible" />
                                            <Setter Target="ExpandCollapseChevron.Padding" Value="0,0,6,0" />
                                            <Setter Target="ContentPresenterGrid.Padding" Value="0" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="TreeViewMultiSelectEnabledSelected">
                                        <VisualState.Setters>
                                            <Setter Target="MultiSelectCheckBox.Visibility" Value="Visible" />
                                            <Setter Target="MultiSelectGrid.Background" Value="{ThemeResource TreeViewItemBackgroundSelected}" />
                                            <Setter Target="MultiSelectGrid.BorderBrush" Value="{ThemeResource TreeViewItemMultiSelectBorderBrushSelected}" />
                                            <Setter Target="ExpandCollapseChevron.Padding" Value="0,0,6,0" />
                                            <Setter Target="ContentPresenterGrid.Padding" Value="0" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="DragStates">
                                    <VisualState x:Name="NotDragging" />
                                    <VisualState x:Name="MultipleDraggingPrimary">
                                        <VisualState.Setters>
                                            <Setter Target="MultiSelectCheckBox.Opacity" Value="0" />
                                            <Setter Target="MultiArrangeOverlayTextBorder.Visibility" Value="Visible" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <Rectangle
                                x:Name="SelectionIndicator"
                                Width="3"
                                Height="12"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Fill="{ThemeResource TreeViewItemSelectionIndicatorForeground}"
                                Opacity="0"
                                RadiusX="2"
                                RadiusY="2" />

                            <Grid
                                x:Name="MultiSelectGrid"
                                Margin="{ThemeResource TreeViewItemMultiSelectSelectedItemBorderMargin}"
                                Padding="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TreeViewItemTemplateSettings.Indentation, Converter={StaticResource BoneHierarchyIndentationConverter}}"
                                BorderBrush="Transparent"
                                BorderThickness="{ThemeResource TreeViewItemBorderThemeThickness}"
                                CornerRadius="{ThemeResource ControlCornerRadius}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Grid Grid.Column="0">
                                    <CheckBox
                                        x:Name="MultiSelectCheckBox"
                                        Width="32"
                                        MinWidth="32"
                                        MinHeight="{ThemeResource TreeViewItemMultiSelectCheckBoxMinHeight}"
                                        Margin="10,0,0,0"
                                        VerticalAlignment="Center"
                                        AutomationProperties.AccessibilityView="Raw"
                                        IsTabStop="False"
                                        Visibility="Collapsed" />
                                    <Border
                                        x:Name="MultiArrangeOverlayTextBorder"
                                        MinWidth="20"
                                        Height="20"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        AutomationProperties.AccessibilityView="Raw"
                                        Background="{ThemeResource SystemControlBackgroundAccentBrush}"
                                        BorderBrush="{ThemeResource SystemControlBackgroundChromeWhiteBrush}"
                                        BorderThickness="1"
                                        CornerRadius="{ThemeResource ControlCornerRadius}"
                                        IsHitTestVisible="False"
                                        Visibility="Collapsed">
                                        <TextBlock
                                            x:Name="MultiArrangeOverlayText"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            AutomationProperties.AccessibilityView="Raw"
                                            Foreground="{ThemeResource SystemControlForegroundChromeWhiteBrush}"
                                            IsHitTestVisible="False"
                                            Style="{ThemeResource CaptionTextBlockStyle}"
                                            Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TreeViewItemTemplateSettings.DragItemsCount}" />
                                    </Border>
                                </Grid>

                                <Grid
                                    x:Name="ExpandCollapseChevron"
                                    Grid.Column="1"
                                    Width="Auto"
                                    Padding="4,0"
                                    Background="Transparent"
                                    Opacity="{TemplateBinding GlyphOpacity}">
                                    <TextBlock
                                        x:Name="CollapsedGlyph"
                                        Width="8"
                                        Height="8"
                                        Padding="0"
                                        VerticalAlignment="Center"
                                        AutomationProperties.AccessibilityView="Raw"
                                        FontFamily="{StaticResource SymbolThemeFontFamily}"
                                        FontSize="{TemplateBinding GlyphSize}"
                                        Foreground="{TemplateBinding GlyphBrush}"
                                        IsHitTestVisible="False"
                                        IsTextScaleFactorEnabled="False"
                                        Text="{TemplateBinding CollapsedGlyph}"
                                        Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TreeViewItemTemplateSettings.CollapsedGlyphVisibility}" />
                                    <TextBlock
                                        x:Name="ExpandedGlyph"
                                        Width="8"
                                        Height="8"
                                        Padding="0"
                                        VerticalAlignment="Center"
                                        AutomationProperties.AccessibilityView="Raw"
                                        FontFamily="{StaticResource SymbolThemeFontFamily}"
                                        FontSize="{TemplateBinding GlyphSize}"
                                        Foreground="{TemplateBinding GlyphBrush}"
                                        IsHitTestVisible="False"
                                        IsTextScaleFactorEnabled="False"
                                        Text="{TemplateBinding ExpandedGlyph}"
                                        Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TreeViewItemTemplateSettings.ExpandedGlyphVisibility}" />
                                </Grid>

                                <ContentPresenter
                                    x:Name="ContentPresenter"
                                    Grid.Column="2"
                                    MinHeight="{ThemeResource TreeViewItemContentHeight}"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    ContentTransitions="{TemplateBinding ContentTransitions}"
                                    Foreground="{TemplateBinding Foreground}" />
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="BoneHierarchyTextStyle" TargetType="TextBlock" BasedOn="{StaticResource BodyStrongTextStyle}">
            <Setter Property="FontSize" Value="10" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>
        <DataTemplate x:Key="RigSlotRowTemplate" x:DataType="models:RigSlotEditorItem">
            <Border
                Margin="0,0,0,8"
                Padding="8"
                Background="{x:Bind IsSelected, Mode=OneWay, Converter={StaticResource SelectedSlotBackgroundConverter}}"
                BorderBrush="{x:Bind IsSelected, Mode=OneWay, Converter={StaticResource SelectedSlotBorderConverter}}"
                BorderThickness="1"
                CornerRadius="8"
                Tag="{x:Bind}"
                Tapped="RigSlotRow_Tapped"
                ToolTipService.ToolTip="{x:Bind LogicalBone}">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Column="0"
                        VerticalAlignment="Center"
                        Style="{StaticResource BodyStrongTextStyle}"
                        Text="{x:Bind DisplayName}" />
                    <Border
                        Grid.Column="1"
                        Background="{x:Bind HasDuplicateAssignment, Mode=OneWay, Converter={StaticResource DuplicateAssignmentBackgroundConverter}}"
                        BorderBrush="{x:Bind HasDuplicateAssignment, Mode=OneWay, Converter={StaticResource DuplicateAssignmentBorderConverter}}"
                        Style="{StaticResource RigSlotAssignedFieldStyle}"
                        ToolTipService.ToolTip="{x:Bind AssignmentToolTip, Mode=OneWay}">
                        <TextBlock
                            Style="{StaticResource CaptionTextStyle}"
                            Foreground="{x:Bind HasDuplicateAssignment, Mode=OneWay, Converter={StaticResource DuplicateAssignmentForegroundConverter}}"
                            Text="{x:Bind AssignedBoneDisplay, Mode=OneWay}"
                            HorizontalAlignment="Right"
                            TextAlignment="Right"
                            TextWrapping="NoWrap"
                            TextTrimming="CharacterEllipsis" />
                    </Border>
                    <Button
                        Grid.Column="2"
                        MinWidth="64"
                        Click="BindSelectedBoneToSlotButton_Click"
                        Content="Bind"
                        Tag="{x:Bind}"
                        ToolTipService.ToolTip="Bind the selected bone to this slot" />
                </Grid>
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="RigSlotSectionTemplate" x:DataType="models:RigSlotSectionItem">
            <Expander IsExpanded="{x:Bind IsExpanded, Mode=TwoWay}" Style="{StaticResource RigSectionExpanderStyle}">
                <Expander.Header>
                    <Grid HorizontalAlignment="Stretch" MinHeight="20">
                        <TextBlock
                            Style="{StaticResource CaptionTextStyle}"
                            FontSize="12"
                            FontWeight="Normal"
                            Text="{x:Bind Title}" />
                    </Grid>
                </Expander.Header>
                <Border Style="{StaticResource SubtlePanelBorderStyle}" Padding="12">
                    <ItemsControl ItemTemplate="{StaticResource RigSlotRowTemplate}" ItemsSource="{x:Bind Slots}" />
                </Border>
            </Expander>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Border x:Name="NoProjectPanel" Style="{StaticResource PanelBorderStyle}" Padding="20">
            <StackPanel Padding="24" Spacing="20">
                <StackPanel Spacing="6">
                    <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="Rigging" />
                    <TextBlock
                        Style="{StaticResource CaptionTextStyle}"
                        Text="No project is currently active. Create or open one from the Home page." />
                </StackPanel>
            </StackPanel>
        </Border>

        <!--Panel shown if project exists-->
        <Grid x:Name="RiggingSettingsPanel" Padding="24" RowSpacing="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Spacing="6">
                <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="Rigging" />
                <TextBlock
                    Style="{StaticResource CaptionTextStyle}"
                    Text="Map imported bones onto the Source rig slots and inspect the imported armature structure." />
            </StackPanel>

            <Grid Grid.Row="1" ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--Our actual bones-->
                    <Border Grid.Column="0" Style="{StaticResource PanelBorderStyle}" Padding="20">
                        <Grid RowSpacing="4">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Style="{StaticResource SectionTitleTextStyle}" Text="Bone Hierarchy" />

                            <Border x:Name="NoSkeletonPanel" Grid.Row="1" Style="{StaticResource SubtlePanelBorderStyle}" Padding="16">
                                <TextBlock
                                    Style="{StaticResource CaptionTextStyle}"
                                    Text="No imported skeleton is currently available. Import a model to inspect and assign bones." />
                            </Border>

                            <ScrollViewer
                                x:Name="BoneHierarchyScrollViewer"
                                Grid.Row="2"
                                HorizontalScrollBarVisibility="Disabled"
                                HorizontalScrollMode="Disabled"
                                VerticalScrollBarVisibility="Auto"
                                VerticalScrollMode="Auto">
                                <StackPanel x:Name="BoneHierarchyPanel" Spacing="12">

                                    <Border Style="{StaticResource SubtlePanelBorderStyle}" Padding="16">
                                        <StackPanel Spacing="8">
                                            <TextBlock
                                                x:Name="SelectedBoneTextBlock"
                                                Style="{StaticResource CaptionTextStyle}"
                                                Text="Selected bone: None" />
                                            <TextBlock
                                                Style="{StaticResource CaptionTextStyle}"
                                                Text="Select a bone in the hierarchy, then use Bind on the rig slot you want to map it to."
                                                TextWrapping="WrapWholeWords" />
                                        </StackPanel>
                                    </Border>

                                    <ScrollViewer
                                        x:Name="BoneHierarchyTreeScrollViewer"
                                        Height="600"
                                        HorizontalScrollBarVisibility="Auto"
                                        HorizontalScrollMode="Auto"
                                        VerticalScrollBarVisibility="Auto"
                                        VerticalScrollMode="Auto">
                                        <TreeView
                                            x:Name="BoneHierarchyTreeView"
                                            CanReorderItems="False"
                                            ItemContainerStyle="{StaticResource BoneHierarchyTreeItemStyle}"
                                            SelectionChanged="BoneHierarchyTreeView_SelectionChanged">
                                            <TreeView.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock
                                                        Style="{StaticResource BoneHierarchyTextStyle}"
                                                        Text="{Binding}" />
                                                </DataTemplate>
                                            </TreeView.ItemTemplate>
                                        </TreeView>
                                    </ScrollViewer>

                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                    <!--List of slots-->
                    <Border Grid.Column="1" Style="{StaticResource PanelBorderStyle}" Padding="20">
                        <Grid RowSpacing="4">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Style="{StaticResource SectionTitleTextStyle}" Text="Rig Slots" />
                                <Button
                                    x:Name="UnbindSelectedButton"
                                    Grid.Column="1"
                                    Click="UnbindSelectedButton_Click"
                                    Content="Unbind Selected"
                                    IsEnabled="False" />
                                <Button
                                    x:Name="UnbindAllButton"
                                    Grid.Column="2"
                                    Click="UnbindAllButton_Click"
                                    Content="Unbind All"
                                    IsEnabled="False" />
                            </Grid>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <ItemsControl
                                    MinHeight="600"
                                    ItemTemplate="{StaticResource RigSlotSectionTemplate}"
                                    ItemsSource="{x:Bind SlotSections, Mode=OneWay}" />
                            </ScrollViewer>
                        </Grid>
                    </Border>
            </Grid>
        </Grid>
    </Grid>
</Page>
